apply plugin: 'java'

version = '1.0'

jar {
    manifest.mainAttributes("Main-Class" : "PosTagger")
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}